session:
    name: 'quipper-servers'
    windows:
        - name: 'databases'
          root: '~/Work/Quipper'
          clear: true
          panes:
            - cmd: 'redis-server'
            - cmd: 'mongod --config /usr/local/etc/mongod.conf'
